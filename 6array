#include<stdio.h>
 int main()
 {
     int n;
     scanf("%d",&n);
     int arr[n];
     int i;
     for(i=0;i<n;i++)
	 { 
	    scanf("%d",&arr[i]);
	 }
	int e=0 ,o=0;
	for(i=0;i<n;i++)
	{ 
	 if(arr[i]%2==0)
	{
		e=e+arr[i];
	}
	else
	{
	    o=o+arr[i];	
	}
}
     printf("%d %d",e,o);
}
	  
