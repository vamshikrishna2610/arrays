#include<stdio.h>
 int main()
 {
     int n;
     scanf("%d",&n);
     int arr[n];
     int i;
     //reading
     for(i=0;i<n;i++)
	 { 
	    scanf("%d",&arr[i]);
	 }
	 //printing
	 for(i=0;i<n;i++)
	 {
	 	printf("%d\n",arr[i]*arr[i]);
	 }
	 return 0;
}
