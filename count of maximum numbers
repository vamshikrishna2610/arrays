
#include<stdio.h>
 int main()
 {
 	int n;
 	scanf("%d",&n);
 	int arr[n];
 	int i;
 	for(i=0;i<n;i++)
 	{
 		scanf("%d",&arr[i]);
	 }
	 int max=arr[0],count=0;
	 for (i=1;i<n;i++)
	{
	  if (arr[i]==max)
 	   count=count+1;
   }
       printf("%d",count);
 	 
 }
